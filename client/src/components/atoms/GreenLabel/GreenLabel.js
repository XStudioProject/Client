import styled from 'styled-components';

const GreenLabel = styled.div`
  background-color: ${({ theme }) => theme.colors.home.greenLabel};
  height: 10px;
  width: 176px;
`;

export default GreenLabel;
